import { MaterialPersianDateAdapter } from './material-persian-date-adapter';

describe('MaterialPersianDateAdapter', () => {
  it('should create an instance', () => {
    expect(new MaterialPersianDateAdapter()).toBeTruthy();
  });
});
